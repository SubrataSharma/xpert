<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pabitrarista.chatdialog">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="chat.xpert.user.AskScreen2Activity" />

        <activity android:name="chat.xpert.user.MainActivity" />

        <activity android:name="chat.xpert.user.XpertListActivity" />

        <activity
            android:name="chat.xpert.user.OtpActivity"
            android:theme="@style/AppTheme2" />

        <activity
            android:name="chat.xpert.user.LoginActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>